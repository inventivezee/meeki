inputs:
  platform:
    required: false
    default: ""
runs:
  using: "composite"
  steps:
    - id: toolchain
      run: |
        echo "channel=$(grep 'channel' rust-toolchain.toml | cut -d'"' -f2)" >> $GITHUB_OUTPUT
        echo "components=$(grep 'components' rust-toolchain.toml | sed 's/.*\[//' | sed 's/\].*//' | tr -d '"' | tr -d ' ')" >> $GITHUB_OUTPUT
      shell: bash
    - id: cache-key
      run: |
        if command -v ldd &> /dev/null; then
          GLIBC_VER=$(ldd --version 2>/dev/null | head -n1 | awk '{print $NF}')
          echo "suffix=-glibc-${GLIBC_VER}" >> $GITHUB_OUTPUT
        else
          echo "suffix=" >> $GITHUB_OUTPUT
        fi
      shell: bash
    - if: inputs.platform == 'macos'
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        targets: aarch64-apple-darwin,x86_64-apple-darwin
        components: ${{ steps.toolchain.outputs.components }}
    - if: inputs.platform == 'ios'
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        targets: aarch64-apple-ios,x86_64-apple-ios,aarch64-apple-ios-sim
        components: ${{ steps.toolchain.outputs.components }}
    - if: inputs.platform == 'android'
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android
        components: ${{ steps.toolchain.outputs.components }}
    - if: inputs.platform == 'windows'
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        targets: x86_64-pc-windows-msvc
        components: ${{ steps.toolchain.outputs.components }}
    - if: inputs.platform == 'linux'
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        components: ${{ steps.toolchain.outputs.components }}
    - if: inputs.platform == 'linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libasound2-dev
      shell: bash
    - if: inputs.platform == 'linux-x86_64'
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        targets: x86_64-unknown-linux-gnu
        components: ${{ steps.toolchain.outputs.components }}
    - if: inputs.platform == 'linux-aarch64'
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        targets: aarch64-unknown-linux-gnu
        components: ${{ steps.toolchain.outputs.components }}
    - if: inputs.platform == ''
      # Pinned rather than @master: this action runs in the job that holds
      # APPLE_CERTIFICATE, TAURI_SIGNING_PRIVATE_KEY and CN_API_KEY, and a
      # mutable branch ref there is a supply-chain hole. This SHA is what the
      # green desktop_v0.0.28 build ran.
      uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
      with:
        toolchain: ${{ steps.toolchain.outputs.channel }}
        components: ${{ steps.toolchain.outputs.components }}
    - uses: Swatinem/rust-cache@v2
      with:
        cache-targets: "true"
        prefix-key: v0-rust${{ steps.cache-key.outputs.suffix }}
        # Defaults to false, which means a failed build discards everything it
        # compiled. This workspace is ~1,960 crates, so a release that fails at
        # bundling or notarization made the next attempt pay the full ~35 minute
        # rebuild for a fix that touched neither.
        cache-on-failure: "true"
