[package]
name = "notification"
version = "0.1.0"
edition = "2024"

[features]
default = ["legacy"]
legacy = ["dep:meeki-notification-macos", "dep:meeki-notification-linux"]

[dependencies]
serde = { workspace = true, features = ["derive"] }
tracing = { workspace = true }

meeki-notification-interface = { workspace = true }

[target.'cfg(target_os = "macos")'.dependencies]
meeki-notification-macos = { path = "../notification-macos", package = "notification-macos", optional = true }

[target.'cfg(target_os = "linux")'.dependencies]
meeki-notification-linux = { path = "../notification-linux", package = "notification-linux", optional = true }
