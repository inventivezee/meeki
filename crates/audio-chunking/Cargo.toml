[package]
name = "audio-chunking"
version = "0.1.0"
edition = "2024"

[features]
default = ["vad"]
vad = [
  "dep:futures-util",
  "dep:meeki-audio-interface",
  "dep:meeki-onnx",
  "dep:meeki-vad",
  "dep:pin-project",
]
segmentation = []

[dev-dependencies]
futures-util = { workspace = true }
hound = { workspace = true }
meeki-data = { workspace = true }
rodio = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }

[[example]]
name = "chunking"
required-features = ["vad"]

[dependencies]
meeki-audio-interface = { workspace = true, optional = true }
meeki-onnx = { workspace = true, optional = true }
meeki-vad = { workspace = true, features = ["silero-onnx"], optional = true }

futures-util = { workspace = true, optional = true }
pin-project = { workspace = true, optional = true }
thiserror = { workspace = true }
