[package]
name = "embedding"
version = "0.1.0"
edition = "2024"

[features]
default = ["onnx"]
onnx = ["dep:meeki-onnx", "dep:knf-rs"]
coreml = ["onnx", "meeki-onnx/coreml"]
directml = ["onnx", "meeki-onnx/directml"]

[dependencies]
knf-rs = { git = "https://github.com/thewh1teagle/pyannote-rs", rev = "e23bd29", package = "knf-rs", optional = true }
meeki-onnx = { workspace = true, optional = true }

serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
approx = { workspace = true }
meeki-data = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
