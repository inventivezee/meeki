/**
 * Formerly auto-selected Anarlog Pro (hyprnote) STT/LLM when billing became paid.
 * Meety does not force Anarlog Pro — users configure AssemblyAI / local models instead.
 */
export async function configurePaidSettings(): Promise<void> {
  // no-op
}
