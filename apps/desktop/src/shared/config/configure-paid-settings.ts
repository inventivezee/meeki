/**
 * Formerly auto-selected Meeki Pro (hyprnote) STT/LLM when billing became paid.
 * Meeki does not force Meeki Pro — users configure AssemblyAI / local models instead.
 */
export async function configurePaidSettings(): Promise<void> {
  // no-op
}
